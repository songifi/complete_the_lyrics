<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>New Notification</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .notification {
            border-left: 4px solid #007bff;
            padding: 15px;
            background-color: #f8f9fa;
            margin: 10px 0;
        }
        .priority-high {
            border-left-color: #dc3545;
        }
        .priority-urgent {
            border-left-color: #fd7e14;
        }
    </style>
</head>
<body>
    <div class="notification {{#if priority}}priority-{{priority}}{{/if}}">
        <h2>{{title}}</h2>
        <p>{{content}}</p>
        
        {{#if metadata}}
        <div class="metadata">
            <small>
                Category: {{category}} | 
                Type: {{type}} |
                Created: {{createdAt}}
            </small>
        </div>
        {{/if}}
    </div>
</body>
</html>
